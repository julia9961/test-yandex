const src = '.';
const dest = '.';

module.exports = {
    path: {
        src: {
            base: src,
            sass: src + '/sass'
        },
        dest: {
            base: dest,
            css: dest + '/css'
        }
    }
};